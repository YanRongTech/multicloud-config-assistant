web:
  serviceName: multi-cloud-content-service
  port: 3005
dataSource:
  database: content
  host: ${db_host}
  port: ${db_port}
  username: ${db_username}
  password: ${db_password}
  pool:
    max: 100
    min: 10
    idle: 60000